package com.billing.pos.customer

/**
 * The app's own technical support line — kept separate from any shop's contact number. Shown
 * only inside the customer app's technical-error dialog (order save / catalog fetch failures),
 * never anywhere else in the app.
 */
object TechnicalSupport {
    const val PHONE = "9961128378"
}
