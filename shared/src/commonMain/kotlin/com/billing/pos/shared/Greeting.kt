package com.billing.pos.shared

/** Placeholder proving the shared module builds and is reachable from :desktopApp — replaced by
 * real shared business logic (Repository, AccountingEngine, entities/DAOs) once this build
 * pipeline (shared module + desktop app + installer) is confirmed working end to end. */
fun greeting(): String = "POS Billing — Windows build pipeline OK"
